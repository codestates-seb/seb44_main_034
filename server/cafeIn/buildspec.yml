version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - echo Build Starting on `date`
      - cd DeployServer
      - chmod +x ./gradlew
      - ./gradlew build
  post_build:
    commands:
      - echo $(basename ./cafeIn/build/libs/*.jar)
artifacts:
  files:
    - cafeIn/build/libs/*.jar
    - cafeIn/scripts/**
    - cafeIn/appspec.yml
  discard-paths: yes