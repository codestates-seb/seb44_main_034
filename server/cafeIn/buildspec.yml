version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - echo Build Starting on `date`
      - cd DeployServer
      - chmod +x ./gradlew
      - ./gradlew build
  post_build:
    commands:
      - echo $(basename ./server/cafeIn/build/libs/*.jar)
artifacts:
  files:
    - server/cafeIn/build/libs/*.jar
    - server/cafeIn/scripts/**
    - server/cafeIn/appspec.yml
  discard-paths: yes
